
export const COLLATERAL_PERCENTAGE = 0.20; // 20% of stake is collateral
export const FALLBACK_CAP_PER_USER_PER_POOL = 1000; // Max fallback amount per user in a pool
export const UNSTAKE_TIME_LOCK_DURATION_MS = 24 * 60 * 60 * 1000; // 24 hours for unstake if debt active (simulated)
export const LP_TOKEN_NAME_PREFIX = "AegisLP-"; // Prefix for LP token names
export const REWARD_DISTRIBUTION_INTERVAL_MS = 60 * 60 * 1000; // 1 hour for rewards (for demo)
export const BASE_APY = 5; // Base 5% APY